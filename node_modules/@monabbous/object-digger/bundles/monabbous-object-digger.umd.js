(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
    typeof define === 'function' && define.amd ? define('@monabbous/object-digger', ['exports'], factory) :
    (global = global || self, factory((global.monabbous = global.monabbous || {}, global.monabbous['object-digger'] = {})));
}(this, (function (exports) { 'use strict';

    function digger(str, context) {
        return (str.match(/[\w\.]+/g) || [])
            .map(function (accessor) {
            return accessor.split('.')
                .reduce(function (a, i) { return typeof a === 'object' ? a[i] : a; }, context);
        });
    }
    function diggerStringParser(diggerString, context, type) {
        if (type === void 0) { type = 'squared-brackets'; }
        var regex = /\[\[\s*[\w.]+\s*]]/g;
        switch (type) {
            case 'backticks':
                regex = /``\s*[\w.]+\s*``/g;
                break;
            case 'curly-brackets':
                regex = /{{\s*[\w.]+\s*}}/g;
                break;
        }
        return diggerString.replace(regex, function (r) { return digger(r, context).shift(); });
    }

    exports.digger = digger;
    exports.diggerStringParser = diggerStringParser;

    Object.defineProperty(exports, '__esModule', { value: true });

})));
//# sourceMappingURL=monabbous-object-digger.umd.js.map
